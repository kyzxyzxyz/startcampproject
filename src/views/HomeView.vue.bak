<template>
  <div class="home-page">
    <section class="hero card">
      <div class="hero-left">
        <h2>구미 · 경북권 추천 여행정보</h2>
        <p class="lead">금오산에서 맛집까지 — 제공된 지역 데이터를 바탕으로 여행 코스를 추천합니다.</p>
        <div class="hero-actions">
          <button class="btn primary" @click="$router.push('/board')">커뮤니티 보기</button>
          <button class="btn ghost" @click="$refs.map?.scrollIntoView({behavior:'smooth'})">지도 보기</button>
        </div>
      </div>
      <div class="hero-right">
        <div class="stats">
          <div class="stat">
            <div class="num">499</div><div class="label">관광지</div>
          </div>
          <div class="stat">
            <div class="num">394</div><div class="label">음식점</div>
          </div>
          <div class="stat">
            <div class="num">411</div><div class="label">쇼핑</div>
          </div>
        </div>
      </div>
    </section>

    <section class="content-row">
      <div class="left-col">
        <div class="card" id="map-section" ref="map">
          <h3 class="card-title">지도</h3>
          <MapView />
        </div>

        <div class="card small-cards">
          <h4>빠른 바로가기</h4>
          <div class="chips">
            <button class="chip" @click="$router.push('/board')">내 장소 의견 남기기</button>
            <button class="chip" @click="$router.push('/chat')">챗봇과 대화</button>
          </div>
        </div>
      </div>

      <aside class="right-col">
        <div class="card">
          <h4>추천 여행코스</h4>
          <ul class="compact-list">
            <li>금오산 산책 코스 · 1일</li>
            <li>구미 푸드 페스티벌 즐기기</li>
            <li>지역 문화시설 탐방</li>
          </ul>
        </div>

        <div class="card">
          <h4>최근 게시글</h4>
          <ul class="compact-list">
            <li><a href="/board">금오산 전망대 근처 맛집 추천</a></li>
            <li><a href="/board">이번 주말 축제 후기</a></li>
            <li><a href="/board">가족 나들이 코스</a></li>
          </ul>
        </div>
      </aside>
    </section>
  </div>
</template>

<script>
import MapView from '../components/MapView.vue'
export default { components: { MapView } }
</script>